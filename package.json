{
  "name": "custom-web-gateway",
  "version": "1.0.0",
  "description": "A custom edge router for web traffic",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Build successful'"
  },
  "devDependencies": {
    "netlify-cli": "latest"
  }
}
